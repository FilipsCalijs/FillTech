// Централизованный файл размеров — все отступы, контейнеры и скругления берутся отсюда

// ─── Контейнеры ────────────────────────────────────────────────────────────
// Стандартная ширина для разных страниц
export const CONTAINER = {
  // Блог-сетка (широкий): desktop ~1152px
  blog: 'max-w-6xl mx-auto px-4 sm:px-6 lg:px-8',

  // Статья / узкий текстовый контент: desktop ~768px
  post: 'max-w-3xl mx-auto px-4 sm:px-6',

  // Дефолтный контент: desktop ~1024px
  default: 'max-w-5xl mx-auto px-4 sm:px-6 lg:px-8',
};

// ─── Скругления ────────────────────────────────────────────────────────────
export const RADIUS = {
  // Карточки блога, общие блоки
  card: 'rounded-2xl',

  // Только верхние углы (например обложка внутри карточки)
  imageTop: 'rounded-t-2xl',

  // Кнопки, инпуты
  button: 'rounded-lg',

  // Маленькие теги / бейджи
  badge: 'rounded-full',
};

// ─── Сетки ─────────────────────────────────────────────────────────────────
export const GRID = {
  // Блог: 1 → 2 → 3 колонки
  blog: 'grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6',
};
